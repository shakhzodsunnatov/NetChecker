# API Flows Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build scenarios from already-captured requests and run them with one tap, so a chain like login → create order → pay → verify can be executed and shared without hand-coding a mock scene and shipping it to TestFlight.

**Architecture:** A flow is a directed acyclic graph of steps. A step runs once every step it depends on has finished and its condition holds — that one rule covers strict ordering, parallel start, wait-for-all and fire-and-forget. Values extracted from responses are injected into later requests through a run context that survives a failure, so a retry resumes instead of restarting. The engine (`FlowRunner`) has no SwiftUI dependency and is tested on its own; the UI renders the graph in computed levels.

**Tech Stack:** Swift 5.9, SwiftUI, Swift Concurrency (`TaskGroup`), XCTest. No third-party dependencies — the package has none and must keep none.

## Global Constraints

- Platforms: iOS 16+, macOS 13+. `ContentUnavailableView`, `\.editMode`, `.listStyle(.insetGrouped)` are unavailable and must not be used.
- Zero third-party dependencies.
- All UI-facing singletons are `@MainActor`; engine types are `Sendable`.
- Doc comments (`///`) on public API in Russian; identifiers in English; commit messages in English with no `Co-Authored-By` trailer.
- Files stay under 500 lines.
- Persistence goes through `UserDefaults`, matching `MockEngine`, `TrafficTagger` and `TaggedRequestArchive`.
- Every task ends green: `swift build` and `swift test` both pass before commit.
- Git flow: work on `feature/api-flows`, squash-merge into `development`.

---

## File Structure

| File | Responsibility |
|---|---|
| `Sources/NetCheckerTraffic/Flows/FlowValueBinding.swift` | Extracting a value from a response and injecting it into a request |
| `Sources/NetCheckerTraffic/Flows/FlowCondition.swift` | Operands and operators for step conditions |
| `Sources/NetCheckerTraffic/Flows/FlowStep.swift` | One step: request, dependencies, condition, inputs, outputs |
| `Sources/NetCheckerTraffic/Flows/Flow.swift` | The graph: level computation, cycle detection |
| `Sources/NetCheckerTraffic/Flows/FlowRunContext.swift` | Values collected during a run |
| `Sources/NetCheckerTraffic/Flows/FlowRunner.swift` | Executes the graph, records outcomes, supports resume |
| `Sources/NetCheckerTraffic/Flows/FlowStore.swift` | Storage and persistence of flows |
| `Sources/NetCheckerTraffic/Flows/FlowExporter.swift` | Serialising a flow for sharing, with secrets stripped |
| `Sources/NetCheckerTrafficUI/Flows/FlowListView.swift` | List of flows, create and delete |
| `Sources/NetCheckerTrafficUI/Flows/FlowCanvasView.swift` | The graph drawn in levels, run control |
| `Sources/NetCheckerTrafficUI/Flows/FlowNodeView.swift` | One node and its states |
| `Sources/NetCheckerTrafficUI/Flows/FlowEdgeView.swift` | Connector with value labels |
| `Sources/NetCheckerTrafficUI/Flows/FlowStepDetailView.swift` | Step outcome and recovery actions |
| `Sources/NetCheckerTrafficUI/Flows/FlowStepPickerView.swift` | Adding steps from captured traffic |

Engine and UI are split so the DAG semantics — the part most likely to be wrong — are testable without a running app.

---

## Task 1: Value binding

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowValueBinding.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowValueBindingTests.swift`

**Interfaces:**
- Consumes: `ResponseData`, `RequestData` (existing).
- Produces: `FlowValueSource`, `FlowValueTarget`, `FlowOutput`, `FlowInput`, `FlowValueExtractor.extract(_:from:)`, `FlowValueInjector.apply(_:value:to:)`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

final class FlowValueBindingTests: XCTestCase {

    private func response(_ json: String, headers: [String: String] = [:]) -> ResponseData {
        ResponseData(statusCode: 200, headers: headers, body: Data(json.utf8))
    }

    func testExtractsTopLevelField() {
        let output = FlowOutput(name: "token", source: .jsonPath("token"))
        let value = FlowValueExtractor.extract(output, from: response(#"{"token":"abc"}"#))
        XCTAssertEqual(value, "abc")
    }

    func testExtractsNestedField() {
        let output = FlowOutput(name: "id", source: .jsonPath("data.order.id"))
        let value = FlowValueExtractor.extract(output, from: response(#"{"data":{"order":{"id":7781}}}"#))
        XCTAssertEqual(value, "7781")
    }

    func testExtractsHeader() {
        let output = FlowOutput(name: "trace", source: .header("X-Trace"))
        let value = FlowValueExtractor.extract(output, from: response("{}", headers: ["X-Trace": "t-1"]))
        XCTAssertEqual(value, "t-1")
    }

    func testMissingFieldYieldsNil() {
        let output = FlowOutput(name: "token", source: .jsonPath("nope"))
        XCTAssertNil(FlowValueExtractor.extract(output, from: response(#"{"token":"abc"}"#)))
    }

    func testInjectsHeader() {
        var request = RequestData(url: URL(string: "https://a.com/x")!, method: .get)
        FlowValueInjector.apply(FlowInput(name: "token", target: .header("Authorization")),
                                value: "Bearer abc", to: &request)
        XCTAssertEqual(request.headers["Authorization"], "Bearer abc")
    }

    func testInjectsPathPlaceholder() {
        var request = RequestData(url: URL(string: "https://a.com/orders/{orderId}/pay")!, method: .post)
        FlowValueInjector.apply(FlowInput(name: "orderId", target: .pathPlaceholder("orderId")),
                                value: "7781", to: &request)
        XCTAssertEqual(request.url.absoluteString, "https://a.com/orders/7781/pay")
    }

    func testInjectsQueryItem() {
        var request = RequestData(url: URL(string: "https://a.com/search")!, method: .get)
        FlowValueInjector.apply(FlowInput(name: "q", target: .queryItem("q")), value: "shoes", to: &request)
        XCTAssertEqual(request.url.absoluteString, "https://a.com/search?q=shoes")
    }

    func testInjectsBodyField() {
        var request = RequestData(url: URL(string: "https://a.com/pay")!, method: .post,
                                  body: Data(#"{"orderId":0}"#.utf8))
        FlowValueInjector.apply(FlowInput(name: "orderId", target: .bodyField("orderId")),
                                value: "7781", to: &request)
        let body = try! JSONSerialization.jsonObject(with: request.body!) as! [String: Any]
        XCTAssertEqual(body["orderId"] as? String, "7781")
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowValueBindingTests`
Expected: FAIL — `cannot find 'FlowOutput' in scope`

- [ ] **Step 3: Write the implementation**

```swift
import Foundation

/// Откуда берётся значение из ответа шага
public enum FlowValueSource: Codable, Sendable, Hashable {
    /// Путь по JSON-телу, сегменты через точку: `data.order.id`
    case jsonPath(String)
    /// Заголовок ответа
    case header(String)
}

/// Куда значение подставляется в следующий запрос
public enum FlowValueTarget: Codable, Sendable, Hashable {
    case header(String)
    /// Плейсхолдер в пути: `/orders/{orderId}/pay`
    case pathPlaceholder(String)
    case queryItem(String)
    /// Поле JSON-тела верхнего уровня
    case bodyField(String)
}

/// Значение, извлекаемое из ответа шага
public struct FlowOutput: Codable, Sendable, Hashable, Identifiable {
    public var id: UUID
    public var name: String
    public var source: FlowValueSource

    public init(id: UUID = UUID(), name: String, source: FlowValueSource) {
        self.id = id
        self.name = name
        self.source = source
    }
}

/// Значение, подставляемое в запрос шага
public struct FlowInput: Codable, Sendable, Hashable, Identifiable {
    public var id: UUID
    /// Имя значения, объявленного выходом одного из предыдущих шагов
    public var name: String
    public var target: FlowValueTarget

    public init(id: UUID = UUID(), name: String, target: FlowValueTarget) {
        self.id = id
        self.name = name
        self.target = target
    }
}

/// Извлечение значений из ответа
public enum FlowValueExtractor {

    /// Достать значение. `nil` означает «в ответе такого нет» —
    /// вызывающая сторона обязана считать это ошибкой шага, а не пустой строкой
    public static func extract(_ output: FlowOutput, from response: ResponseData) -> String? {
        switch output.source {
        case .header(let name):
            let key = name.lowercased()
            for (headerName, value) in response.headers where headerName.lowercased() == key {
                return value
            }
            return nil

        case .jsonPath(let path):
            guard let body = response.body,
                  let root = try? JSONSerialization.jsonObject(with: body) else { return nil }
            return value(at: path, in: root)
        }
    }

    private static func value(at path: String, in root: Any) -> String? {
        var current: Any? = root

        for segment in path.split(separator: ".") {
            guard let dictionary = current as? [String: Any] else { return nil }
            current = dictionary[String(segment)]
        }

        switch current {
        case let text as String: return text
        case let number as NSNumber: return describe(number)
        case .none: return nil
        default: return nil
        }
    }

    /// NSNumber одинаково представляет и число, и булево — различаем по типу
    private static func describe(_ number: NSNumber) -> String {
        if CFGetTypeID(number) == CFBooleanGetTypeID() {
            return number.boolValue ? "true" : "false"
        }
        if number.doubleValue == number.doubleValue.rounded() {
            return String(number.intValue)
        }
        return String(number.doubleValue)
    }
}

/// Подстановка значений в запрос
public enum FlowValueInjector {

    public static func apply(_ input: FlowInput, value: String, to request: inout RequestData) {
        switch input.target {
        case .header(let name):
            request.headers[name] = value

        case .pathPlaceholder(let name):
            let replaced = request.url.absoluteString
                .replacingOccurrences(of: "{\(name)}", with: value)
            if let url = URL(string: replaced) { request.url = url }

        case .queryItem(let name):
            guard var components = URLComponents(url: request.url, resolvingAgainstBaseURL: false) else { return }
            var items = components.queryItems ?? []
            items.removeAll { $0.name == name }
            items.append(URLQueryItem(name: name, value: value))
            components.queryItems = items
            if let url = components.url { request.url = url }

        case .bodyField(let name):
            guard let body = request.body,
                  var object = (try? JSONSerialization.jsonObject(with: body)) as? [String: Any] else { return }
            object[name] = value
            guard let updated = try? JSONSerialization.data(withJSONObject: object) else { return }
            request.body = updated
            request.bodySize = Int64(updated.count)
        }
    }
}
```

`RequestData.url` is currently a `var`, so injection can rewrite it — confirm before writing; if it is `let`, widen it in this task and note it in the commit.

- [ ] **Step 4: Run test to verify it passes**

Run: `swift test --filter FlowValueBindingTests`
Expected: PASS, 8 tests

- [ ] **Step 5: Commit**

```bash
git checkout -b feature/api-flows
git add Sources/NetCheckerTraffic/Flows/FlowValueBinding.swift Tests/NetCheckerTrafficTests/FlowValueBindingTests.swift
git commit -m "feat: extract values from flow responses and inject them into requests

A missing field returns nil rather than an empty string: sending a request with
a blank substitution produces a confusing server error instead of a clear one."
```

---

## Task 2: Conditions

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowCondition.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowConditionTests.swift`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: `FlowCondition`, `FlowConditionOperator`, `FlowCondition.evaluate(values:) -> Bool`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

final class FlowConditionTests: XCTestCase {

    private let values = ["total": "120", "status": "paid", "flag": "true", "empty": ""]

    func testEqualsMatches() {
        let condition = FlowCondition(valueName: "status", operator: .equals, operand: "paid")
        XCTAssertTrue(condition.evaluate(values: values))
    }

    func testEqualsFailsOnDifferentValue() {
        let condition = FlowCondition(valueName: "status", operator: .equals, operand: "failed")
        XCTAssertFalse(condition.evaluate(values: values))
    }

    func testNotEquals() {
        XCTAssertTrue(FlowCondition(valueName: "status", operator: .notEquals, operand: "failed")
            .evaluate(values: values))
    }

    func testNumericComparison() {
        XCTAssertTrue(FlowCondition(valueName: "total", operator: .greaterThan, operand: "0")
            .evaluate(values: values))
        XCTAssertFalse(FlowCondition(valueName: "total", operator: .lessThan, operand: "0")
            .evaluate(values: values))
    }

    func testNonNumericComparisonIsFalse() {
        XCTAssertFalse(FlowCondition(valueName: "status", operator: .greaterThan, operand: "0")
            .evaluate(values: values))
    }

    func testIsTrueAndIsFalse() {
        XCTAssertTrue(FlowCondition(valueName: "flag", operator: .isTrue).evaluate(values: values))
        XCTAssertFalse(FlowCondition(valueName: "flag", operator: .isFalse).evaluate(values: values))
    }

    func testExistsAndIsEmpty() {
        XCTAssertTrue(FlowCondition(valueName: "status", operator: .exists).evaluate(values: values))
        XCTAssertFalse(FlowCondition(valueName: "missing", operator: .exists).evaluate(values: values))
        XCTAssertTrue(FlowCondition(valueName: "empty", operator: .isEmpty).evaluate(values: values))
    }

    func testMissingValueIsFalseForComparisons() {
        XCTAssertFalse(FlowCondition(valueName: "missing", operator: .equals, operand: "x")
            .evaluate(values: values))
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowConditionTests`
Expected: FAIL — `cannot find 'FlowCondition' in scope`

- [ ] **Step 3: Write the implementation**

```swift
import Foundation

/// Оператор сравнения в условии шага
public enum FlowConditionOperator: String, Codable, Sendable, CaseIterable {
    case equals
    case notEquals
    case greaterThan
    case lessThan
    case isTrue
    case isFalse
    case exists
    case isEmpty

    public var title: String {
        switch self {
        case .equals: return "равно"
        case .notEquals: return "не равно"
        case .greaterThan: return "больше"
        case .lessThan: return "меньше"
        case .isTrue: return "истина"
        case .isFalse: return "ложь"
        case .exists: return "существует"
        case .isEmpty: return "пусто"
        }
    }

    /// Оператору нужен правый операнд
    public var needsOperand: Bool {
        switch self {
        case .equals, .notEquals, .greaterThan, .lessThan: return true
        case .isTrue, .isFalse, .exists, .isEmpty: return false
        }
    }
}

/// Условие выполнения шага.
///
/// Хранится на шаге, а рисуется на входящей связи: хранить проще,
/// читается как развилка. Ложное условие означает пропуск, а не ошибку.
public struct FlowCondition: Codable, Sendable, Hashable {
    /// Имя значения, собранного предыдущими шагами
    public var valueName: String
    public var `operator`: FlowConditionOperator
    public var operand: String?

    public init(valueName: String, operator: FlowConditionOperator, operand: String? = nil) {
        self.valueName = valueName
        self.operator = `operator`
        self.operand = operand
    }

    /// Человекочитаемое описание для подписи на связи
    public var summary: String {
        guard `operator`.needsOperand, let operand = operand else {
            return "\(valueName) \(`operator`.title)"
        }
        return "\(valueName) \(`operator`.title) \(operand)"
    }

    public func evaluate(values: [String: String]) -> Bool {
        switch `operator` {
        case .exists:
            return values[valueName] != nil
        case .isEmpty:
            return values[valueName]?.isEmpty ?? true
        default:
            break
        }

        guard let value = values[valueName] else { return false }

        switch `operator` {
        case .equals:
            return value == operand
        case .notEquals:
            return value != operand
        case .isTrue:
            return value.lowercased() == "true"
        case .isFalse:
            return value.lowercased() == "false"
        case .greaterThan, .lessThan:
            guard let left = Double(value), let right = operand.flatMap(Double.init) else { return false }
            return `operator` == .greaterThan ? left > right : left < right
        case .exists, .isEmpty:
            return false
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `swift test --filter FlowConditionTests`
Expected: PASS, 8 tests

- [ ] **Step 5: Commit**

```bash
git add Sources/NetCheckerTraffic/Flows/FlowCondition.swift Tests/NetCheckerTrafficTests/FlowConditionTests.swift
git commit -m "feat: add step conditions for flow branching

Comparing a non-numeric value with > or < is false rather than a crash, and a
missing value fails every comparison instead of being treated as empty."
```

---

## Task 3: Step and graph

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowStep.swift`
- Create: `Sources/NetCheckerTraffic/Flows/Flow.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowGraphTests.swift`

**Interfaces:**
- Consumes: `FlowCondition`, `FlowInput`, `FlowOutput` (Tasks 1–2), `RequestData`.
- Produces: `FlowStep`, `Flow`, `Flow.levels() -> [[FlowStep]]`, `Flow.hasCycle: Bool`, `Flow.step(id:)`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

final class FlowGraphTests: XCTestCase {

    private func step(_ name: String, dependsOn: [UUID] = []) -> FlowStep {
        FlowStep(
            name: name,
            request: RequestData(url: URL(string: "https://api.example.com/\(name)")!, method: .get),
            dependsOn: dependsOn
        )
    }

    func testStepsWithoutDependenciesShareTheFirstLevel() {
        let a = step("a"), b = step("b")
        let flow = Flow(name: "f", steps: [a, b])

        let levels = flow.levels()
        XCTAssertEqual(levels.count, 1)
        XCTAssertEqual(levels[0].count, 2)
    }

    func testDependencyCreatesASecondLevel() {
        let a = step("a")
        let b = step("b", dependsOn: [a.id])
        let flow = Flow(name: "f", steps: [a, b])

        let levels = flow.levels()
        XCTAssertEqual(levels.count, 2)
        XCTAssertEqual(levels[0].map(\.name), ["a"])
        XCTAssertEqual(levels[1].map(\.name), ["b"])
    }

    func testStepWaitingForTwoLandsAfterBoth() {
        let a = step("a"), b = step("b")
        let c = step("c", dependsOn: [a.id, b.id])
        let flow = Flow(name: "f", steps: [a, b, c])

        let levels = flow.levels()
        XCTAssertEqual(levels.count, 2)
        XCTAssertEqual(levels[1].map(\.name), ["c"])
    }

    func testFireAndForgetStepIsDetected() {
        let a = step("a"), trigger = step("trigger")
        let b = step("b", dependsOn: [a.id])
        let flow = Flow(name: "f", steps: [a, trigger, b])

        XCTAssertTrue(flow.isFireAndForget(trigger))
        XCTAssertFalse(flow.isFireAndForget(a))
    }

    func testCycleIsDetected() {
        var a = step("a")
        var b = step("b")
        a.dependsOn = [b.id]
        b.dependsOn = [a.id]
        let flow = Flow(name: "f", steps: [a, b])

        XCTAssertTrue(flow.hasCycle)
        XCTAssertTrue(flow.levels().isEmpty)
    }

    func testAcyclicGraphReportsNoCycle() {
        let a = step("a")
        let b = step("b", dependsOn: [a.id])
        XCTAssertFalse(Flow(name: "f", steps: [a, b]).hasCycle)
    }

    func testStepLookupById() {
        let a = step("a")
        XCTAssertEqual(Flow(name: "f", steps: [a]).step(id: a.id)?.name, "a")
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowGraphTests`
Expected: FAIL — `cannot find 'FlowStep' in scope`

- [ ] **Step 3: Write `FlowStep.swift`**

```swift
import Foundation

/// Шаг сценария — один запрос вместе со своими связями
public struct FlowStep: Identifiable, Codable, Sendable, Hashable {
    public var id: UUID
    public var name: String

    /// Запрос, снятый с пойманного трафика
    public var request: RequestData

    /// Шаги, завершения которых он ждёт
    public var dependsOn: [UUID]

    /// Условие выполнения. Ложное — шаг пропускается, но не считается ошибкой
    public var condition: FlowCondition?

    /// Что извлечь из ответа
    public var outputs: [FlowOutput]

    /// Что подставить в запрос
    public var inputs: [FlowInput]

    /// Ожидаемый статус ответа. Несовпадение — ошибка шага
    public var expectedStatusCode: Int?

    /// Пауза перед запуском, секунды
    public var delay: TimeInterval

    public init(
        id: UUID = UUID(),
        name: String,
        request: RequestData,
        dependsOn: [UUID] = [],
        condition: FlowCondition? = nil,
        outputs: [FlowOutput] = [],
        inputs: [FlowInput] = [],
        expectedStatusCode: Int? = nil,
        delay: TimeInterval = 0
    ) {
        self.id = id
        self.name = name
        self.request = request
        self.dependsOn = dependsOn
        self.condition = condition
        self.outputs = outputs
        self.inputs = inputs
        self.expectedStatusCode = expectedStatusCode
        self.delay = max(0, delay)
    }
}
```

- [ ] **Step 4: Write `Flow.swift`**

```swift
import Foundation

/// Сценарий — ациклический граф шагов
public struct Flow: Identifiable, Codable, Sendable, Hashable {
    public var id: UUID
    public var name: String
    public var steps: [FlowStep]
    public var createdAt: Date

    public init(id: UUID = UUID(), name: String, steps: [FlowStep] = [], createdAt: Date = Date()) {
        self.id = id
        self.name = name
        self.steps = steps
        self.createdAt = createdAt
    }

    public func step(id: UUID) -> FlowStep? {
        steps.first { $0.id == id }
    }

    /// Никто не ждёт этот шаг — «триггер на всякий случай»
    public func isFireAndForget(_ step: FlowStep) -> Bool {
        !steps.contains { $0.dependsOn.contains(step.id) }
    }

    /// Граф содержит цикл и не может быть выполнен
    public var hasCycle: Bool {
        levelsIfAcyclic() == nil
    }

    /// Уровни выполнения: всё внутри уровня идёт одновременно.
    /// Пустой массив означает цикл — выполнять такой граф нельзя.
    public func levels() -> [[FlowStep]] {
        levelsIfAcyclic() ?? []
    }

    /// Топологическая сортировка по уровням (алгоритм Кана).
    /// `nil` означает цикл: остались шаги, у которых зависимости так и не разрешились.
    private func levelsIfAcyclic() -> [[FlowStep]]? {
        let known = Set(steps.map(\.id))
        var remaining = steps
        var resolved = Set<UUID>()
        var result: [[FlowStep]] = []

        while !remaining.isEmpty {
            // Ссылки на несуществующие шаги игнорируются: иначе удаление шага
            // намертво заклинивало бы весь сценарий
            let ready = remaining.filter { step in
                step.dependsOn.allSatisfy { !known.contains($0) || resolved.contains($0) }
            }

            guard !ready.isEmpty else { return nil }

            result.append(ready)
            resolved.formUnion(ready.map(\.id))
            let readyIds = Set(ready.map(\.id))
            remaining.removeAll { readyIds.contains($0.id) }
        }

        return result
    }
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `swift test --filter FlowGraphTests`
Expected: PASS, 7 tests

- [ ] **Step 6: Commit**

```bash
git add Sources/NetCheckerTraffic/Flows/FlowStep.swift Sources/NetCheckerTraffic/Flows/Flow.swift Tests/NetCheckerTrafficTests/FlowGraphTests.swift
git commit -m "feat: model flows as a DAG with computed execution levels

Levels come from Kahn's algorithm, so parallel start, wait-for-all and
fire-and-forget all fall out of the dependency edges without a separate mode
switch. A dependency on a deleted step is ignored rather than jamming the whole
graph, and a cycle yields no levels so it can be refused before running."
```

---

## Task 4: Run context

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowRunContext.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowRunContextTests.swift`

**Interfaces:**
- Consumes: `FlowStep`, `FlowOutput`, `FlowInput`, `ResponseData`, `RequestData`.
- Produces: `FlowStepOutcome`, `FlowStepState`, `FlowRunContext` with `values`, `record(_:for:)`, `prepared(_:) throws -> RequestData`, `FlowRunError`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

final class FlowRunContextTests: XCTestCase {

    private func step(inputs: [FlowInput] = [], outputs: [FlowOutput] = []) -> FlowStep {
        FlowStep(
            name: "s",
            request: RequestData(url: URL(string: "https://a.com/orders/{orderId}")!, method: .get),
            outputs: outputs,
            inputs: inputs
        )
    }

    func testRecordingOutputsMakesValuesAvailable() {
        var context = FlowRunContext()
        let source = step(outputs: [FlowOutput(name: "orderId", source: .jsonPath("id"))])

        context.record(ResponseData(statusCode: 200, body: Data(#"{"id":7781}"#.utf8)), for: source)

        XCTAssertEqual(context.values["orderId"], "7781")
    }

    func testPreparedRequestSubstitutesValues() throws {
        var context = FlowRunContext()
        context.values["orderId"] = "7781"

        let target = step(inputs: [FlowInput(name: "orderId", target: .pathPlaceholder("orderId"))])
        let request = try context.prepared(target)

        XCTAssertEqual(request.url.absoluteString, "https://a.com/orders/7781")
    }

    func testMissingValueThrowsInsteadOfSendingBlank() {
        let context = FlowRunContext()
        let target = step(inputs: [FlowInput(name: "orderId", target: .pathPlaceholder("orderId"))])

        XCTAssertThrowsError(try context.prepared(target)) { error in
            XCTAssertEqual(error as? FlowRunError, .missingValue("orderId"))
        }
    }

    func testMissingOutputInResponseIsReportedAsUnresolved() {
        var context = FlowRunContext()
        let source = step(outputs: [FlowOutput(name: "orderId", source: .jsonPath("nope"))])

        context.record(ResponseData(statusCode: 200, body: Data("{}".utf8)), for: source)

        XCTAssertNil(context.values["orderId"])
        XCTAssertEqual(context.unresolvedOutputs, ["orderId"])
    }

    func testContextSurvivesAcrossSteps() {
        var context = FlowRunContext()
        context.record(ResponseData(statusCode: 200, body: Data(#"{"token":"abc"}"#.utf8)),
                       for: step(outputs: [FlowOutput(name: "token", source: .jsonPath("token"))]))
        context.record(ResponseData(statusCode: 201, body: Data(#"{"id":1}"#.utf8)),
                       for: step(outputs: [FlowOutput(name: "orderId", source: .jsonPath("id"))]))

        XCTAssertEqual(context.values.count, 2)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowRunContextTests`
Expected: FAIL — `cannot find 'FlowRunContext' in scope`

- [ ] **Step 3: Write the implementation**

```swift
import Foundation

/// Ошибка выполнения шага
public enum FlowRunError: Error, Equatable, Sendable {
    /// Значение не найдено среди собранных
    case missingValue(String)
    /// Ответ не совпал с ожидаемым статусом
    case unexpectedStatus(expected: Int, actual: Int)
    /// Сетевая ошибка
    case transport(String)

    public var message: String {
        switch self {
        case .missingValue(let name):
            return "Значение «\(name)» отсутствует в ответах предыдущих шагов"
        case .unexpectedStatus(let expected, let actual):
            return "Ожидался статус \(expected), получен \(actual)"
        case .transport(let text):
            return text
        }
    }
}

/// Состояние шага в прогоне
public enum FlowStepState: Codable, Sendable, Hashable {
    case pending
    case running
    case succeeded
    case failed(String)
    case skipped
    case notRun
}

/// Итог одного шага
public struct FlowStepOutcome: Identifiable, Sendable, Hashable {
    public var id: UUID { stepId }
    public let stepId: UUID
    public var state: FlowStepState
    public var statusCode: Int?
    public var duration: TimeInterval
    /// Что подставилось в запрос — обычно ломается именно это
    public var substitutions: [String: String]

    public init(
        stepId: UUID,
        state: FlowStepState = .pending,
        statusCode: Int? = nil,
        duration: TimeInterval = 0,
        substitutions: [String: String] = [:]
    ) {
        self.stepId = stepId
        self.state = state
        self.statusCode = statusCode
        self.duration = duration
        self.substitutions = substitutions
    }
}

/// Значения, собранные за прогон.
///
/// Переживает падение шага: именно поэтому «Повторить с шага N»
/// не заставляет логиниться заново.
public struct FlowRunContext: Sendable {
    public private(set) var values: [String: String] = [:]

    /// Выходы, которых не оказалось в ответе
    public private(set) var unresolvedOutputs: [String] = []

    public init() {}

    /// Записать выходы шага
    public mutating func record(_ response: ResponseData, for step: FlowStep) {
        for output in step.outputs {
            if let value = FlowValueExtractor.extract(output, from: response) {
                values[output.name] = value
            } else if !unresolvedOutputs.contains(output.name) {
                unresolvedOutputs.append(output.name)
            }
        }
    }

    /// Собрать запрос шага с подстановками.
    /// Бросает, если значения нет: уходить в сеть с пустой подстановкой нельзя
    public func prepared(_ step: FlowStep) throws -> RequestData {
        var request = step.request

        for input in step.inputs {
            guard let value = values[input.name] else {
                throw FlowRunError.missingValue(input.name)
            }
            FlowValueInjector.apply(input, value: value, to: &request)
        }

        return request
    }

    /// Подстановки шага для показа в разборе
    public func substitutions(for step: FlowStep) -> [String: String] {
        var result: [String: String] = [:]
        for input in step.inputs {
            result[input.name] = values[input.name] ?? "—"
        }
        return result
    }

    public mutating func reset() {
        values.removeAll()
        unresolvedOutputs.removeAll()
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `swift test --filter FlowRunContextTests`
Expected: PASS, 5 tests

- [ ] **Step 5: Commit**

```bash
git add Sources/NetCheckerTraffic/Flows/FlowRunContext.swift Tests/NetCheckerTrafficTests/FlowRunContextTests.swift
git commit -m "feat: carry collected values through a flow run

The context outlives a failed step, which is what lets a retry resume from that
step instead of restarting from login. A missing value throws rather than
substituting an empty string, so the failure names the value instead of
surfacing as a confusing server error."
```

---

## Task 5: Runner

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowRunner.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowRunnerTests.swift`

**Interfaces:**
- Consumes: everything from Tasks 1–4, `RequestRetrier.retry(request:) -> RetryResult`.
- Produces: `FlowExecuting` protocol, `FlowRunner` (`@MainActor`, `ObservableObject`) with `outcomes`, `isRunning`, `failedStepId`, `run(_:)`, `retry(from:in:)`, `skip(_:in:)`.

`FlowExecuting` exists so tests drive the runner without network access. The real implementation delegates to `RequestRetrier`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

/// Подставной исполнитель: сеть в тестах не нужна, а нужен контроль ответов
private actor StubExecutor: FlowExecuting {
    var responses: [String: (ResponseData?, FlowRunError?)] = [:]
    private(set) var calls: [String] = []

    init(responses: [String: (ResponseData?, FlowRunError?)]) {
        self.responses = responses
    }

    func execute(_ request: RequestData) async -> (ResponseData?, FlowRunError?) {
        calls.append(request.url.path)
        return responses[request.url.path] ?? (ResponseData(statusCode: 200, body: Data("{}".utf8)), nil)
    }

    func recordedCalls() -> [String] { calls }
}

@MainActor
final class FlowRunnerTests: XCTestCase {

    private func step(_ path: String, dependsOn: [UUID] = [], condition: FlowCondition? = nil,
                      outputs: [FlowOutput] = [], inputs: [FlowInput] = [],
                      expected: Int? = nil) -> FlowStep {
        FlowStep(
            name: path,
            request: RequestData(url: URL(string: "https://api.example.com\(path)")!, method: .get),
            dependsOn: dependsOn,
            condition: condition,
            outputs: outputs,
            inputs: inputs,
            expectedStatusCode: expected
        )
    }

    func testRunsEveryStep() async {
        let a = step("/a"), b = step("/b", dependsOn: [])
        let runner = FlowRunner(executor: StubExecutor(responses: [:]))

        await runner.run(Flow(name: "f", steps: [a, b]))

        XCTAssertEqual(runner.outcomes[a.id]?.state, .succeeded)
        XCTAssertEqual(runner.outcomes[b.id]?.state, .succeeded)
        XCTAssertFalse(runner.isRunning)
    }

    func testValuePassesBetweenSteps() async {
        let first = step("/login", outputs: [FlowOutput(name: "token", source: .jsonPath("token"))])
        let second = step("/me", dependsOn: [first.id],
                          inputs: [FlowInput(name: "token", target: .header("Authorization"))])

        let executor = StubExecutor(responses: [
            "/login": (ResponseData(statusCode: 200, body: Data(#"{"token":"abc"}"#.utf8)), nil)
        ])
        let runner = FlowRunner(executor: executor)

        await runner.run(Flow(name: "f", steps: [first, second]))

        XCTAssertEqual(runner.outcomes[second.id]?.state, .succeeded)
        XCTAssertEqual(runner.context.values["token"], "abc")
    }

    func testFailureStopsDependentsButNotSiblings() async {
        let failing = step("/bad")
        let dependent = step("/after", dependsOn: [failing.id])
        let sibling = step("/independent")

        let executor = StubExecutor(responses: [
            "/bad": (nil, FlowRunError.transport("offline"))
        ])
        let runner = FlowRunner(executor: executor)

        await runner.run(Flow(name: "f", steps: [failing, dependent, sibling]))

        XCTAssertEqual(runner.outcomes[dependent.id]?.state, .notRun)
        XCTAssertEqual(runner.outcomes[sibling.id]?.state, .succeeded)
        XCTAssertEqual(runner.failedStepId, failing.id)
    }

    func testUnexpectedStatusFailsTheStep() async {
        let checked = step("/x", expected: 200)
        let executor = StubExecutor(responses: [
            "/x": (ResponseData(statusCode: 500, body: Data("{}".utf8)), nil)
        ])
        let runner = FlowRunner(executor: executor)

        await runner.run(Flow(name: "f", steps: [checked]))

        guard case .failed = runner.outcomes[checked.id]?.state else {
            return XCTFail("Ожидалось падение шага по несовпавшему статусу")
        }
    }

    func testFalseConditionSkipsAndCascades() async {
        let source = step("/status", outputs: [FlowOutput(name: "paid", source: .jsonPath("paid"))])
        let branch = step("/pay", dependsOn: [source.id],
                          condition: FlowCondition(valueName: "paid", operator: .isTrue))
        let after = step("/receipt", dependsOn: [branch.id])

        let executor = StubExecutor(responses: [
            "/status": (ResponseData(statusCode: 200, body: Data(#"{"paid":false}"#.utf8)), nil)
        ])
        let runner = FlowRunner(executor: executor)

        await runner.run(Flow(name: "f", steps: [source, branch, after]))

        XCTAssertEqual(runner.outcomes[branch.id]?.state, .skipped)
        XCTAssertEqual(runner.outcomes[after.id]?.state, .skipped)
    }

    func testIfElseRunsExactlyOneBranch() async {
        let source = step("/status", outputs: [FlowOutput(name: "paid", source: .jsonPath("paid"))])
        let ifTrue = step("/paid", dependsOn: [source.id],
                          condition: FlowCondition(valueName: "paid", operator: .isTrue))
        let ifFalse = step("/unpaid", dependsOn: [source.id],
                           condition: FlowCondition(valueName: "paid", operator: .isFalse))

        let executor = StubExecutor(responses: [
            "/status": (ResponseData(statusCode: 200, body: Data(#"{"paid":true}"#.utf8)), nil)
        ])
        let runner = FlowRunner(executor: executor)

        await runner.run(Flow(name: "f", steps: [source, ifTrue, ifFalse]))

        XCTAssertEqual(runner.outcomes[ifTrue.id]?.state, .succeeded)
        XCTAssertEqual(runner.outcomes[ifFalse.id]?.state, .skipped)
    }

    func testMissingValueFailsWithNamedValue() async {
        let target = step("/pay", inputs: [FlowInput(name: "orderId", target: .pathPlaceholder("orderId"))])
        let runner = FlowRunner(executor: StubExecutor(responses: [:]))

        await runner.run(Flow(name: "f", steps: [target]))

        guard case .failed(let message) = runner.outcomes[target.id]?.state else {
            return XCTFail("Ожидалось падение шага")
        }
        XCTAssertTrue(message.contains("orderId"))
    }

    func testRetryFromStepKeepsEarlierValues() async {
        let first = step("/login", outputs: [FlowOutput(name: "token", source: .jsonPath("token"))])
        let failing = step("/pay", dependsOn: [first.id],
                           inputs: [FlowInput(name: "token", target: .header("Authorization"))])

        let executor = StubExecutor(responses: [
            "/login": (ResponseData(statusCode: 200, body: Data(#"{"token":"abc"}"#.utf8)), nil),
            "/pay": (nil, FlowRunError.transport("offline"))
        ])
        let runner = FlowRunner(executor: executor)
        let flow = Flow(name: "f", steps: [first, failing])

        await runner.run(flow)
        XCTAssertEqual(runner.failedStepId, failing.id)

        await executor.setResponse(for: "/pay", to: (ResponseData(statusCode: 200, body: Data("{}".utf8)), nil))
        await runner.retry(from: failing.id, in: flow)

        XCTAssertEqual(runner.outcomes[failing.id]?.state, .succeeded)
        // Логин не должен выполняться повторно
        let calls = await executor.recordedCalls()
        XCTAssertEqual(calls.filter { $0 == "/login" }.count, 1)
    }

    func testSkipContinuesTheRun() async {
        let failing = step("/bad")
        let after = step("/after", dependsOn: [failing.id])

        let executor = StubExecutor(responses: ["/bad": (nil, FlowRunError.transport("offline"))])
        let runner = FlowRunner(executor: executor)
        let flow = Flow(name: "f", steps: [failing, after])

        await runner.run(flow)
        await runner.skip(failing.id, in: flow)

        XCTAssertEqual(runner.outcomes[failing.id]?.state, .skipped)
        XCTAssertEqual(runner.outcomes[after.id]?.state, .succeeded)
    }

    func testCyclicFlowIsRefused() async {
        var a = step("/a"), b = step("/b")
        a.dependsOn = [b.id]
        b.dependsOn = [a.id]

        let runner = FlowRunner(executor: StubExecutor(responses: [:]))
        await runner.run(Flow(name: "f", steps: [a, b]))

        XCTAssertTrue(runner.outcomes.isEmpty)
        XCTAssertNotNil(runner.refusalReason)
    }
}

private extension StubExecutor {
    func setResponse(for path: String, to value: (ResponseData?, FlowRunError?)) {
        responses[path] = value
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowRunnerTests`
Expected: FAIL — `cannot find 'FlowRunner' in scope`

- [ ] **Step 3: Write the implementation**

```swift
import Foundation
import Combine

/// Отправка запроса шага. Отдельный протокол, чтобы прогон
/// проверялся без обращения к сети
public protocol FlowExecuting: Sendable {
    func execute(_ request: RequestData) async -> (ResponseData?, FlowRunError?)
}

/// Боевой исполнитель поверх существующего повтора запросов
public struct LiveFlowExecutor: FlowExecuting {
    public init() {}

    public func execute(_ request: RequestData) async -> (ResponseData?, FlowRunError?) {
        let result = await RequestRetrier.retry(request: request)

        if let error = result.error {
            return (nil, .transport(error.localizedDescription))
        }
        return (result.response, nil)
    }
}

/// Выполнение сценария
@MainActor
public final class FlowRunner: ObservableObject {
    @Published public private(set) var outcomes: [UUID: FlowStepOutcome] = [:]
    @Published public private(set) var isRunning = false
    @Published public private(set) var failedStepId: UUID?
    /// Причина отказа выполнять сценарий, например цикл в графе
    @Published public private(set) var refusalReason: String?

    public private(set) var context = FlowRunContext()

    private let executor: FlowExecuting

    public init(executor: FlowExecuting = LiveFlowExecutor()) {
        self.executor = executor
    }

    // MARK: - Прогон

    /// Выполнить сценарий с нуля
    public func run(_ flow: Flow) async {
        guard !flow.hasCycle else {
            refusalReason = "Сценарий содержит цикл и не может быть выполнен"
            return
        }

        refusalReason = nil
        failedStepId = nil
        context.reset()
        outcomes = Dictionary(uniqueKeysWithValues: flow.steps.map { ($0.id, FlowStepOutcome(stepId: $0.id)) })

        await execute(flow, startingFrom: nil)
    }

    /// Повторить с указанного шага, сохранив уже собранные значения
    public func retry(from stepId: UUID, in flow: Flow) async {
        failedStepId = nil
        await execute(flow, startingFrom: stepId)
    }

    /// Пометить шаг пропущенным и продолжить
    public func skip(_ stepId: UUID, in flow: Flow) async {
        outcomes[stepId]?.state = .skipped
        failedStepId = nil
        await execute(flow, startingFrom: stepId, skipping: stepId)
    }

    // MARK: - Ядро

    private func execute(_ flow: Flow, startingFrom resumeId: UUID?, skipping skipId: UUID? = nil) async {
        isRunning = true
        defer { isRunning = false }

        var reached = resumeId == nil

        for level in flow.levels() {
            // Уровень целиком идёт параллельно: шаги внутри него
            // друг от друга не зависят по построению
            await withTaskGroup(of: (UUID, FlowStepOutcome).self) { group in
                for step in level {
                    if let resumeId = resumeId, !reached {
                        if step.id == resumeId { reached = true } else { continue }
                    }
                    if step.id == skipId { continue }
                    guard shouldRun(step, in: flow) else {
                        outcomes[step.id]?.state = .skipped
                        continue
                    }

                    let prepared = context.prepared(step)
                    let substitutions = context.substitutions(for: step)
                    outcomes[step.id]?.state = .running

                    group.addTask { [executor] in
                        await Self.perform(step, prepared: prepared, substitutions: substitutions, executor: executor)
                    }
                }

                for await (id, outcome) in group {
                    outcomes[id] = outcome
                    if case .failed = outcome.state, failedStepId == nil {
                        failedStepId = id
                    }
                    if case .succeeded = outcome.state,
                       let step = flow.step(id: id),
                       let response = lastResponses[id] {
                        context.record(response, for: step)
                    }
                }
            }

            // Падение останавливает прогон: иначе посыпались бы все следующие
            // шаги и стало бы неясно, что сломалось на самом деле
            if failedStepId != nil { break }
        }

        markUnreached(flow)
    }

    /// Шаг выполняется, если его зависимости успешны и условие истинно
    private func shouldRun(_ step: FlowStep, in flow: Flow) -> Bool {
        for dependency in step.dependsOn {
            guard flow.step(id: dependency) != nil else { continue }
            guard case .succeeded = outcomes[dependency]?.state else { return false }
        }

        guard let condition = step.condition else { return true }
        return condition.evaluate(values: context.values)
    }

    private func markUnreached(_ flow: Flow) {
        for step in flow.steps where outcomes[step.id]?.state == .pending {
            outcomes[step.id]?.state = .notRun
        }
    }

    private var lastResponses: [UUID: ResponseData] = [:]

    private static func perform(
        _ step: FlowStep,
        prepared: Result<RequestData, Error>,
        substitutions: [String: String],
        executor: FlowExecuting
    ) async -> (UUID, FlowStepOutcome) {
        var outcome = FlowStepOutcome(stepId: step.id, substitutions: substitutions)

        let request: RequestData
        switch prepared {
        case .success(let value): request = value
        case .failure(let error):
            outcome.state = .failed((error as? FlowRunError)?.message ?? error.localizedDescription)
            return (step.id, outcome)
        }

        if step.delay > 0 {
            try? await Task.sleep(nanoseconds: UInt64(step.delay * 1_000_000_000))
        }

        let started = Date()
        let (response, error) = await executor.execute(request)
        outcome.duration = Date().timeIntervalSince(started)

        if let error = error {
            outcome.state = .failed(error.message)
            return (step.id, outcome)
        }

        guard let response = response else {
            outcome.state = .failed(FlowRunError.transport("Пустой ответ").message)
            return (step.id, outcome)
        }

        outcome.statusCode = response.statusCode

        if let expected = step.expectedStatusCode, expected != response.statusCode {
            outcome.state = .failed(FlowRunError.unexpectedStatus(expected: expected,
                                                                 actual: response.statusCode).message)
            return (step.id, outcome)
        }

        outcome.state = .succeeded
        return (step.id, outcome)
    }
}
```

Two details to settle while implementing, both visible in the tests above: `context.prepared(step)` throws, so wrap it as `Result` before handing it to the task group; and the response needed by `context.record` must travel back with the outcome rather than through `lastResponses` — carry it in the tuple returned by `perform` and drop that dictionary.

- [ ] **Step 4: Run test to verify it passes**

Run: `swift test --filter FlowRunnerTests`
Expected: PASS, 10 tests

- [ ] **Step 5: Commit**

```bash
git add Sources/NetCheckerTraffic/Flows/FlowRunner.swift Tests/NetCheckerTrafficTests/FlowRunnerTests.swift
git commit -m "feat: execute flows level by level with resume and skip

Steps in a level run concurrently through a task group; a failure stops the run
so the first broken step stays identifiable instead of being buried under
cascading failures. Retry and skip reuse the collected context, so resuming does
not repeat the login. Execution goes through a FlowExecuting protocol so the
semantics are tested without touching the network."
```

---

## Task 6: Storage and export

**Files:**
- Create: `Sources/NetCheckerTraffic/Flows/FlowStore.swift`
- Create: `Sources/NetCheckerTraffic/Flows/FlowExporter.swift`
- Test: `Tests/NetCheckerTrafficTests/FlowStoreTests.swift`

**Interfaces:**
- Consumes: `Flow`, `FlowStep`, `ArchivedRequest`, `TrafficRecord`.
- Produces: `FlowStore.shared` with `flows`, `add`, `update`, `remove`, `flow(id:)`, `makeFlow(name:from:)`; `FlowExporter.export(_:) -> Data`, `FlowExporter.load(_:) throws -> Flow`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

@MainActor
final class FlowStoreTests: XCTestCase {

    private var store: FlowStore { FlowStore.shared }

    override func setUp() async throws {
        try await super.setUp()
        store.removeAll()
    }

    override func tearDown() async throws {
        store.removeAll()
        try await super.tearDown()
    }

    private func record(_ path: String) -> TrafficRecord {
        TrafficRecord(
            request: RequestData(url: URL(string: "https://api.example.com\(path)")!,
                                 method: .post,
                                 headers: ["Authorization": "Bearer secret"])
        )
    }

    func testAddAndLookup() {
        let flow = Flow(name: "Checkout")
        store.add(flow)

        XCTAssertEqual(store.flows.count, 1)
        XCTAssertEqual(store.flow(id: flow.id)?.name, "Checkout")
    }

    func testRemove() {
        let flow = Flow(name: "Checkout")
        store.add(flow)
        store.remove(id: flow.id)

        XCTAssertTrue(store.flows.isEmpty)
    }

    func testBuildingFlowFromRecordsKeepsOrderAndChainsThem() {
        let flow = store.makeFlow(name: "Checkout", from: [record("/login"), record("/orders"), record("/pay")])

        XCTAssertEqual(flow.steps.count, 3)
        XCTAssertEqual(flow.steps[0].dependsOn, [])
        XCTAssertEqual(flow.steps[1].dependsOn, [flow.steps[0].id])
        XCTAssertEqual(flow.steps[2].dependsOn, [flow.steps[1].id])
        XCTAssertEqual(flow.levels().count, 3)
    }

    func testStepKeepsHeadersAndBody() {
        let flow = store.makeFlow(name: "f", from: [record("/login")])
        XCTAssertEqual(flow.steps[0].request.headers["Authorization"], "Bearer secret")
    }

    func testExportStripsSensitiveHeaders() throws {
        let flow = store.makeFlow(name: "f", from: [record("/login")])
        let data = FlowExporter.export(flow)
        let text = String(data: data, encoding: .utf8) ?? ""

        // Файл со сценарием уходит другому человеку — живой токен уходить не должен
        XCTAssertFalse(text.contains("Bearer secret"))
    }

    func testExportedFlowLoadsBack() throws {
        let flow = store.makeFlow(name: "Checkout", from: [record("/login"), record("/pay")])
        let loaded = try FlowExporter.load(FlowExporter.export(flow))

        XCTAssertEqual(loaded.name, "Checkout")
        XCTAssertEqual(loaded.steps.count, 2)
        XCTAssertEqual(loaded.steps[1].dependsOn, [loaded.steps[0].id])
    }

    func testLoadingGarbageThrows() {
        XCTAssertThrowsError(try FlowExporter.load(Data("nope".utf8)))
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter FlowStoreTests`
Expected: FAIL — `cannot find 'FlowStore' in scope`

- [ ] **Step 3: Write `FlowStore.swift`**

```swift
import Foundation

/// Хранилище сценариев
@MainActor
public final class FlowStore: ObservableObject {
    public static let shared = FlowStore()

    @Published public private(set) var flows: [Flow] = [] {
        didSet { persist() }
    }

    private let storageKey = "NetCheckerFlows"

    private init() {
        load()
    }

    public func flow(id: UUID) -> Flow? {
        flows.first { $0.id == id }
    }

    public func add(_ flow: Flow) {
        flows.insert(flow, at: 0)
    }

    public func update(_ flow: Flow) {
        guard let index = flows.firstIndex(where: { $0.id == flow.id }) else { return }
        flows[index] = flow
    }

    public func remove(id: UUID) {
        flows.removeAll { $0.id == id }
    }

    public func removeAll() {
        flows.removeAll()
    }

    /// Собрать сценарий из выбранных записей трафика.
    /// Шаги связываются последовательно — ветвление настраивается потом
    public func makeFlow(name: String, from records: [TrafficRecord]) -> Flow {
        var steps: [FlowStep] = []

        for record in records {
            let step = FlowStep(
                name: "\(record.request.method.rawValue) \(record.path)",
                request: record.request,
                dependsOn: steps.last.map { [$0.id] } ?? []
            )
            steps.append(step)
        }

        return Flow(name: name, steps: steps)
    }

    /// То же из сохранённых помеченных запросов
    public func makeFlow(name: String, from archived: [ArchivedRequest]) -> Flow {
        var steps: [FlowStep] = []

        for request in archived {
            let step = FlowStep(
                name: "\(request.method.rawValue) \(request.path)",
                request: request.requestData,
                dependsOn: steps.last.map { [$0.id] } ?? []
            )
            steps.append(step)
        }

        return Flow(name: name, steps: steps)
    }

    private func persist() {
        guard let data = try? JSONEncoder().encode(flows) else { return }
        UserDefaults.standard.set(data, forKey: storageKey)
    }

    private func load() {
        guard let data = UserDefaults.standard.data(forKey: storageKey),
              let stored = try? JSONDecoder().decode([Flow].self, from: data) else { return }
        flows = stored
    }
}
```

- [ ] **Step 4: Write `FlowExporter.swift`**

```swift
import Foundation

/// Перенос сценария между устройствами
public enum FlowExporter {

    /// Заглушка вместо секрета при экспорте
    public static let redaction = "***REDACTED***"

    /// Сериализовать сценарий для отправки.
    ///
    /// Значения чувствительных заголовков заменяются заглушкой: файл уходит
    /// другому человеку, и живой токен уходить вместе с ним не должен
    public static func export(_ flow: Flow) -> Data {
        var sanitised = flow
        let sensitive = Set(HeaderFormatter.sensitiveHeaders.map { $0.lowercased() })

        sanitised.steps = flow.steps.map { step in
            var copy = step
            copy.request.headers = step.request.headers.reduce(into: [:]) { result, item in
                result[item.key] = sensitive.contains(item.key.lowercased()) ? redaction : item.value
            }
            return copy
        }

        let encoder = JSONEncoder()
        encoder.outputFormatting = [.prettyPrinted, .sortedKeys]
        return (try? encoder.encode(sanitised)) ?? Data()
    }

    public static func load(_ data: Data) throws -> Flow {
        try JSONDecoder().decode(Flow.self, from: data)
    }
}
```

Confirm `HeaderFormatter.sensitiveHeaders` is `public` or `internal` to this module before use; if it is `private`, widen it in this task.

- [ ] **Step 5: Run test to verify it passes**

Run: `swift test --filter FlowStoreTests`
Expected: PASS, 7 tests

- [ ] **Step 6: Commit**

```bash
git add Sources/NetCheckerTraffic/Flows/FlowStore.swift Sources/NetCheckerTraffic/Flows/FlowExporter.swift Tests/NetCheckerTrafficTests/FlowStoreTests.swift
git commit -m "feat: store flows and export them for sharing

Building a flow from selected traffic chains the steps in order; branching is
configured afterwards. Export replaces sensitive header values with a
placeholder, since the file is meant to be handed to someone else and a live
token should not travel with it."
```

---

## Task 7: Node and edge views

**Files:**
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowNodeView.swift`
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowEdgeView.swift`
- Test: manual — SwiftUI views are covered by the UI tests in Task 10.

**Interfaces:**
- Consumes: `FlowStep`, `FlowStepOutcome`, `FlowStepState`, `Flow.isFireAndForget(_:)`.
- Produces: `FlowNodeView(step:outcome:index:isFireAndForget:incomingValues:)`, `FlowEdgeView(labels:state:)`, `FlowEdgeState`.

- [ ] **Step 1: Write `FlowNodeView.swift`**

```swift
import SwiftUI
import NetCheckerTrafficCore

/// Узел графа — один шаг сценария
struct FlowNodeView: View {
    let step: FlowStep
    let outcome: FlowStepOutcome?
    let index: Int
    let isFireAndForget: Bool
    /// Зависимости из неcоседних уровней: длинные дуги не рисуются,
    /// вместо них строка внутри узла
    let incomingValues: [String]

    private var state: FlowStepState { outcome?.state ?? .pending }

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            HStack(spacing: 5) {
                indicator
                NetCheckerTrafficUI_MethodBadge(method: step.request.method)
                Spacer(minLength: 0)
            }

            Text(step.request.url.path.isEmpty ? "/" : step.request.url.path)
                .font(.system(size: 12.5, weight: .semibold))
                .lineLimit(2)
                .multilineTextAlignment(.leading)

            Text(statusText)
                .font(.caption2)
                .foregroundStyle(statusColor)

            if isFireAndForget {
                Text("не ждём")
                    .font(.system(size: 9, weight: .bold))
                    .padding(.horizontal, 5)
                    .padding(.vertical, 1)
                    .background(Color.orange.opacity(0.15))
                    .foregroundStyle(.orange)
                    .clipShape(Capsule())
            }

            if let condition = step.condition {
                Text(condition.summary)
                    .font(.system(size: 9, weight: .semibold))
                    .padding(.horizontal, 5)
                    .padding(.vertical, 1)
                    .background(Color.purple.opacity(0.12))
                    .foregroundStyle(.purple)
                    .clipShape(Capsule())
            }

            ForEach(incomingValues, id: \.self) { value in
                Text("← \(value)")
                    .font(.system(size: 9))
                    .foregroundStyle(.purple)
            }
        }
        .padding(9)
        .frame(maxWidth: .infinity, alignment: .leading)
        .background(Color(white: 1).opacity(0.001))
        .background(.background)
        .clipShape(RoundedRectangle(cornerRadius: 13))
        .overlay(
            RoundedRectangle(cornerRadius: 13)
                .stroke(borderColor, lineWidth: isRunning ? 2 : 1)
        )
        .opacity(isDimmed ? 0.5 : 1)
        // scale, а не изменение размеров: соседние узлы не съезжают
        .scaleEffect(isRunning ? 1.03 : 1)
        .animation(.spring(response: 0.3, dampingFraction: 0.7), value: isRunning)
        .accessibilityElement(children: .combine)
        .accessibilityLabel("Шаг \(index). \(step.name). \(statusText)")
    }

    private var indicator: some View {
        ZStack {
            Circle().fill(indicatorColor).frame(width: 18, height: 18)
            switch state {
            case .succeeded:
                Image(systemName: "checkmark").font(.system(size: 9, weight: .bold)).foregroundStyle(.white)
            case .failed:
                Image(systemName: "exclamationmark").font(.system(size: 9, weight: .bold)).foregroundStyle(.white)
            default:
                Text("\(index)").font(.system(size: 10, weight: .bold)).foregroundStyle(.white)
            }
        }
    }

    private var isRunning: Bool { state == .running }

    private var isDimmed: Bool {
        state == .pending || state == .notRun || state == .skipped
    }

    private var statusText: String {
        switch state {
        case .pending: return "ожидает"
        case .running: return "выполняется…"
        case .succeeded: return outcome?.statusCode.map { "\($0) · \(durationText)" } ?? "готово"
        case .failed(let message): return message
        case .skipped: return "пропущен"
        case .notRun: return "не запускался"
        }
    }

    private var durationText: String {
        let duration = outcome?.duration ?? 0
        return duration < 1 ? "\(Int(duration * 1000)) мс" : String(format: "%.2f с", duration)
    }

    private var statusColor: Color {
        if case .failed = state { return .red }
        return .secondary
    }

    private var indicatorColor: Color {
        switch state {
        case .succeeded: return .green
        case .failed: return .red
        case .running: return .accentColor
        default: return isFireAndForget ? .orange : Color(white: 0.78)
        }
    }

    private var borderColor: Color {
        switch state {
        case .succeeded: return .green.opacity(0.5)
        case .failed: return .red
        case .running: return .accentColor
        default: return isFireAndForget ? .orange.opacity(0.6) : Color.secondary.opacity(0.2)
        }
    }
}
```

- [ ] **Step 2: Write `FlowEdgeView.swift`**

```swift
import SwiftUI

/// Состояние связи между уровнями
enum FlowEdgeState {
    case idle
    case active
    case failed
    case notRun
}

/// Связь между уровнями с подписями передаваемых значений
struct FlowEdgeView: View {
    /// Имена значений; больше двух сворачиваются в «+N»
    let labels: [String]
    let state: FlowEdgeState

    private var caption: String? {
        guard !labels.isEmpty else { return nil }
        if labels.count <= 2 { return labels.joined(separator: " · ") }
        return "\(labels[0]) +\(labels.count - 1)"
    }

    var body: some View {
        ZStack {
            Rectangle()
                .fill(color)
                .frame(width: 2)

            if let caption = caption {
                Text(caption)
                    .font(.system(size: 9, weight: .semibold))
                    .padding(.horizontal, 7)
                    .padding(.vertical, 3)
                    .background(background)
                    .foregroundStyle(color)
                    .clipShape(Capsule())
            }
        }
        .frame(height: 40)
        .accessibilityHidden(caption == nil)
        .accessibilityLabel(caption.map { "Передаётся: \($0)" } ?? "")
    }

    private var color: Color {
        switch state {
        case .idle: return Color.secondary.opacity(0.35)
        case .active: return .purple
        case .failed: return .red
        case .notRun: return Color.secondary.opacity(0.25)
        }
    }

    private var background: Color {
        switch state {
        case .active: return .purple.opacity(0.12)
        case .failed: return .red.opacity(0.12)
        default: return Color.secondary.opacity(0.1)
        }
    }
}
```

- [ ] **Step 3: Verify it builds**

Run: `swift build`
Expected: Build complete, no errors

- [ ] **Step 4: Commit**

```bash
git add Sources/NetCheckerTrafficUI/Flows/FlowNodeView.swift Sources/NetCheckerTrafficUI/Flows/FlowEdgeView.swift
git commit -m "feat: draw flow nodes and edges

A running node scales rather than resizing, so its neighbours stay put. State is
carried by an icon and text as well as colour. Edges name the values they pass
and collapse to +N past two."
```

---

## Task 8: Canvas

**Files:**
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowCanvasView.swift`
- Test: manual — covered by Task 10.

**Interfaces:**
- Consumes: `Flow`, `FlowRunner`, `FlowNodeView`, `FlowEdgeView`.
- Produces: `NetCheckerTrafficUI_FlowCanvasView(flow:)`.

- [ ] **Step 1: Write the view**

```swift
import SwiftUI
import NetCheckerTrafficCore

/// Граф сценария по уровням и управление прогоном
public struct NetCheckerTrafficUI_FlowCanvasView: View {
    let flow: Flow

    @StateObject private var runner = FlowRunner()
    @State private var selectedStep: FlowStep?

    public init(flow: Flow) {
        self.flow = flow
    }

    public var body: some View {
        VStack(spacing: 0) {
            runBar

            if let reason = runner.refusalReason {
                Text(reason)
                    .font(.caption)
                    .foregroundStyle(.red)
                    .padding(8)
            }

            ScrollView {
                VStack(spacing: 0) {
                    ForEach(Array(flow.levels().enumerated()), id: \.offset) { index, level in
                        if index > 0 {
                            FlowEdgeView(labels: valueLabels(into: level), state: edgeState(for: level))
                        }
                        levelRow(level, startingIndex: startIndex(of: index))
                    }
                }
                .padding(.horizontal, 14)
                .padding(.vertical, 16)
            }
        }
        .navigationTitle(flow.name)
        #if os(iOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .sheet(item: $selectedStep) { step in
            NavigationStack {
                NetCheckerTrafficUI_FlowStepDetailView(
                    step: step,
                    outcome: runner.outcomes[step.id],
                    onRetry: { Task { await runner.retry(from: step.id, in: flow) } },
                    onSkip: { Task { await runner.skip(step.id, in: flow) } }
                )
            }
        }
    }

    private var runBar: some View {
        HStack(spacing: 10) {
            Button {
                Task { await runner.run(flow) }
            } label: {
                HStack {
                    Spacer()
                    if runner.isRunning {
                        ProgressView().controlSize(.small)
                        Text("Выполняется…")
                    } else {
                        Image(systemName: "play.fill")
                        Text("Запустить")
                    }
                    Spacer()
                }
                .fontWeight(.semibold)
            }
            .buttonStyle(.borderedProminent)
            .disabled(runner.isRunning || flow.steps.isEmpty)
        }
        .padding(.horizontal, 14)
        .padding(.vertical, 10)
        .background(.thinMaterial)
    }

    /// Строка уровня. Больше двух узлов не помещаются в ширину телефона,
    /// поэтому такая строка прокручивается вбок сама по себе
    @ViewBuilder
    private func levelRow(_ level: [FlowStep], startingIndex: Int) -> some View {
        if level.count > 2 {
            ScrollView(.horizontal, showsIndicators: false) {
                HStack(alignment: .top, spacing: 8) {
                    nodes(level, startingIndex: startingIndex)
                }
            }
        } else {
            HStack(alignment: .top, spacing: 8) {
                nodes(level, startingIndex: startingIndex)
            }
        }
    }

    private func nodes(_ level: [FlowStep], startingIndex: Int) -> some View {
        ForEach(Array(level.enumerated()), id: \.element.id) { offset, step in
            FlowNodeView(
                step: step,
                outcome: runner.outcomes[step.id],
                index: startingIndex + offset + 1,
                isFireAndForget: flow.isFireAndForget(step),
                incomingValues: distantInputs(for: step)
            )
            .frame(width: level.count > 2 ? 158 : nil)
            .contentShape(RoundedRectangle(cornerRadius: 13))
            .onTapGesture { selectedStep = step }
        }
    }

    private func startIndex(of levelIndex: Int) -> Int {
        flow.levels().prefix(levelIndex).reduce(0) { $0 + $1.count }
    }

    /// Значения, приходящие не с соседнего уровня — показываются строкой в узле
    private func distantInputs(for step: FlowStep) -> [String] {
        let levels = flow.levels()
        guard let levelIndex = levels.firstIndex(where: { $0.contains(where: { $0.id == step.id }) }),
              levelIndex > 0 else { return [] }

        let adjacent = Set(levels[levelIndex - 1].map(\.id))
        let distant = step.dependsOn.filter { !adjacent.contains($0) }
        guard !distant.isEmpty else { return [] }

        return step.inputs.map(\.name)
    }

    private func valueLabels(into level: [FlowStep]) -> [String] {
        Array(Set(level.flatMap { $0.inputs.map(\.name) })).sorted()
    }

    private func edgeState(for level: [FlowStep]) -> FlowEdgeState {
        if level.contains(where: { runner.outcomes[$0.id]?.state == .notRun }) { return .notRun }
        if level.contains(where: { if case .failed = runner.outcomes[$0.id]?.state { return true } else { return false } }) { return .failed }
        if level.contains(where: { runner.outcomes[$0.id]?.state == .succeeded }) { return .active }
        return .idle
    }
}
```

- [ ] **Step 2: Verify it builds**

Run: `swift build`
Expected: Build complete, no errors

- [ ] **Step 3: Commit**

```bash
git add Sources/NetCheckerTrafficUI/Flows/FlowCanvasView.swift
git commit -m "feat: draw the flow graph in execution levels

A row is one level, so parallelism is visible without a label. Levels are
computed, so nothing has to be dragged and scrolling stays vertical. A level
wider than two nodes scrolls sideways on its own rather than moving the screen."
```

---

## Task 9: Step detail, picker, flow list, tab

**Files:**
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowStepDetailView.swift`
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowStepPickerView.swift`
- Create: `Sources/NetCheckerTrafficUI/Flows/FlowListView.swift`
- Modify: `Sources/NetCheckerTraffic/Core/InspectorFeatures.swift`
- Modify: `Sources/NetCheckerTrafficUI/Modifiers/TrafficInspectorModifier.swift`
- Test: `Tests/NetCheckerTrafficTests/InspectorFeatureFlowsTests.swift`

**Interfaces:**
- Consumes: `FlowStore`, `FlowRunner`, `FlowStepOutcome`, `TrafficStore`, `TaggedRequestArchive`.
- Produces: `NetCheckerTrafficUI_FlowStepDetailView(step:outcome:onRetry:onSkip:)`, `NetCheckerTrafficUI_FlowListView()`, `InspectorFeature.flows`.

- [ ] **Step 1: Write the failing test**

```swift
import XCTest
@testable import NetCheckerTrafficCore

@MainActor
final class InspectorFeatureFlowsTests: XCTestCase {

    func testFlowsIsAFeature() {
        XCTAssertTrue(InspectorFeature.allCases.contains(.flows))
        XCTAssertEqual(InspectorFeature.flows.title, "Flows")
    }

    func testFlowsCanBeHidden() {
        let settings = InspectorFeatureSettings.shared
        settings.showAll()
        settings.setVisible(false, for: .flows)

        XCTAssertFalse(settings.isVisible(.flows))
        settings.showAll()
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `swift test --filter InspectorFeatureFlowsTests`
Expected: FAIL — `type 'InspectorFeature' has no member 'flows'`

- [ ] **Step 3: Add the feature case**

In `InspectorFeatures.swift`, add `case flows` to `InspectorFeature` and extend the three switches:

```swift
case flows

// title
case .flows: return "Flows"
// systemImage
case .flows: return "point.3.connected.trianglepath.dotted"
// summary
case .flows: return "Сценарии из связанных запросов"
```

`isRequired` stays `self == .traffic`, so Flows can be hidden like the rest.

- [ ] **Step 4: Write `FlowStepDetailView.swift`**

```swift
import SwiftUI
import NetCheckerTrafficCore

/// Разбор шага и действия восстановления
public struct NetCheckerTrafficUI_FlowStepDetailView: View {
    let step: FlowStep
    let outcome: FlowStepOutcome?
    let onRetry: () -> Void
    let onSkip: () -> Void

    @SwiftUI.Environment(\.dismiss) private var dismiss

    public init(step: FlowStep, outcome: FlowStepOutcome?,
                onRetry: @escaping () -> Void, onSkip: @escaping () -> Void) {
        self.step = step
        self.outcome = outcome
        self.onRetry = onRetry
        self.onSkip = onSkip
    }

    public var body: some View {
        List {
            Section("Запрос") {
                LabeledContent("Метод", value: step.request.method.rawValue)
                LabeledContent("URL", value: step.request.url.absoluteString)
            }

            if let outcome = outcome {
                Section("Результат") {
                    if let status = outcome.statusCode {
                        LabeledContent("Статус", value: "\(status)")
                    }
                    LabeledContent("Длительность", value: String(format: "%.0f мс", outcome.duration * 1000))
                    if case .failed(let message) = outcome.state {
                        Text(message).foregroundStyle(.red).font(.caption)
                    }
                }

                if !outcome.substitutions.isEmpty {
                    Section {
                        ForEach(outcome.substitutions.sorted(by: { $0.key < $1.key }), id: \.key) { name, value in
                            LabeledContent(name, value: value)
                                .font(.system(.caption, design: .monospaced))
                        }
                    } header: {
                        Text("Подставлено")
                    } footer: {
                        Text("Чаще всего ломается именно подстановка, а не сам запрос.")
                    }
                }
            }

            if case .failed = outcome?.state {
                Section {
                    Button {
                        onRetry()
                        dismiss()
                    } label: {
                        Label("Повторить с этого шага", systemImage: "arrow.clockwise")
                    }

                    Button {
                        onSkip()
                        dismiss()
                    } label: {
                        Label("Пропустить шаг", systemImage: "forward")
                    }
                } footer: {
                    Text("Значения предыдущих шагов сохранены — заново логиниться не нужно.")
                }
            }
        }
        .navigationTitle(step.name)
        #if os(iOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .toolbar {
            ToolbarItem(placement: .cancellationAction) {
                Button("Готово") { dismiss() }
            }
        }
    }
}
```

- [ ] **Step 5: Write `FlowStepPickerView.swift`**

```swift
import SwiftUI
import NetCheckerTrafficCore

/// Добавление шагов из пойманного трафика — тем же множественным выбором,
/// что и пометка тегами
struct FlowStepPickerView: View {
    let onDone: ([TrafficRecord]) -> Void

    @SwiftUI.Environment(\.dismiss) private var dismiss
    @ObservedObject private var store = TrafficStore.shared
    @State private var selected: [UUID] = []

    var body: some View {
        List {
            ForEach(store.records, id: \.compositeId) { record in
                Button {
                    toggle(record.id)
                } label: {
                    HStack(spacing: 10) {
                        orderBadge(for: record.id)
                        TrafficRecordRow(record: record)
                    }
                    .contentShape(Rectangle())
                }
                .buttonStyle(.plain)
            }
        }
        .listStyle(.plain)
        .navigationTitle("Добавить шаги")
        #if os(iOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .toolbar {
            ToolbarItem(placement: .cancellationAction) {
                Button("Отмена") { dismiss() }
            }
            ToolbarItem(placement: .confirmationAction) {
                Button("Добавить (\(selected.count))") {
                    // Порядок отметки задаёт порядок шагов
                    let records = selected.compactMap { store.record(for: $0) }
                    onDone(records)
                    dismiss()
                }
                .disabled(selected.isEmpty)
            }
        }
    }

    @ViewBuilder
    private func orderBadge(for id: UUID) -> some View {
        if let index = selected.firstIndex(of: id) {
            ZStack {
                Circle().fill(Color.accentColor).frame(width: 22, height: 22)
                Text("\(index + 1)").font(.caption2.bold()).foregroundStyle(.white)
            }
        } else {
            Image(systemName: "circle").foregroundStyle(.secondary).imageScale(.large)
        }
    }

    private func toggle(_ id: UUID) {
        if let index = selected.firstIndex(of: id) {
            selected.remove(at: index)
        } else {
            selected.append(id)
        }
    }
}
```

- [ ] **Step 6: Write `FlowListView.swift`**

```swift
import SwiftUI
import NetCheckerTrafficCore

/// Список сценариев
public struct NetCheckerTrafficUI_FlowListView: View {
    @ObservedObject private var store = FlowStore.shared
    @State private var isNaming = false
    @State private var newName = ""

    public init() {}

    public var body: some View {
        List {
            if store.flows.isEmpty {
                Section { emptyState }
            } else {
                ForEach(store.flows) { flow in
                    NavigationLink {
                        NetCheckerTrafficUI_FlowCanvasView(flow: flow)
                    } label: {
                        VStack(alignment: .leading, spacing: 2) {
                            Text(flow.name).font(.body)
                            Text("\(flow.steps.count) шагов · \(flow.levels().count) уровней")
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                    }
                }
                .onDelete { offsets in
                    for index in offsets { store.remove(id: store.flows[index].id) }
                }
            }
        }
        .navigationTitle("Flows")
        .toolbar {
            ToolbarItem(placement: .primaryAction) {
                Button { isNaming = true } label: { Image(systemName: "plus") }
            }
        }
        .alert("Новый сценарий", isPresented: $isNaming) {
            TextField("Checkout Flow", text: $newName)
            Button("Отмена", role: .cancel) { newName = "" }
            Button("Создать") {
                let name = newName.trimmingCharacters(in: .whitespaces)
                if !name.isEmpty { store.add(Flow(name: name)) }
                newName = ""
            }
        }
    }

    private var emptyState: some View {
        VStack(spacing: 8) {
            Image(systemName: "point.3.connected.trianglepath.dotted")
                .font(.system(size: 32))
                .foregroundStyle(.secondary)
            Text("Нет сценариев").font(.headline)
            Text("Соберите цепочку из пойманных запросов и прогоняйте её одной кнопкой.")
                .font(.caption)
                .foregroundStyle(.secondary)
                .multilineTextAlignment(.center)
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 24)
    }
}
```

- [ ] **Step 7: Wire the tab**

In `TrafficInspectorModifier.swift`, add to the `screen(for:)` switch:

```swift
case .flows:
    NetCheckerTrafficUI_FlowListView()
```

- [ ] **Step 8: Run tests and build**

Run: `swift build && swift test`
Expected: Build complete; all tests pass including the 2 new ones

- [ ] **Step 9: Commit**

```bash
git add Sources/NetCheckerTraffic/Core/InspectorFeatures.swift Sources/NetCheckerTrafficUI/Flows Sources/NetCheckerTrafficUI/Modifiers/TrafficInspectorModifier.swift Tests/NetCheckerTrafficTests/InspectorFeatureFlowsTests.swift
git commit -m "feat: add the Flows tab, step detail and step picker

The step picker numbers rows in the order they are selected, so the order of
selection becomes the order of the steps. The failed-step sheet offers retry and
skip and says that earlier values are kept, since that is the whole point of
resuming rather than restarting. Flows is an ordinary inspector section and can
be hidden like the others."
```

---

## Task 10: UI tests

**Files:**
- Create: `NetChecker-iOS/CheckerTestApp/CheckerTestAppUITests/FlowsUITests.swift`
- Modify: `NetChecker-iOS/CheckerTestApp/CheckerTestApp/ContentView.swift`

**Interfaces:**
- Consumes: the Flows UI from Tasks 7–9.
- Produces: no code interface — regression coverage.

The demo app shows SDK screens as tabs. Add a Flows tab so the UI tests can reach it without the shake gesture.

- [ ] **Step 1: Add the tab to the demo app**

In `ContentView.swift`, after the Traffic tab:

```swift
NavigationStack {
    FlowListView()
}
.tabItem {
    Label("Flows", systemImage: "point.3.connected.trianglepath.dotted")
}
```

Add `public typealias FlowListView = NetCheckerTrafficUI_FlowListView` to `NetCheckerTrafficUI.swift` alongside the existing aliases.

- [ ] **Step 2: Write the UI tests**

```swift
import XCTest

final class FlowsUITests: XCTestCase {

    private var app: XCUIApplication!

    override func setUpWithError() throws {
        continueAfterFailure = false
        app = XCUIApplication()
        app.launch()
    }

    override func tearDownWithError() throws {
        app = nil
    }

    private func makeTraffic() {
        app.tabBars.buttons["API Test"].tap()
        let button = app.buttons
            .matching(NSPredicate(format: "label CONTAINS %@", "Single Post"))
            .firstMatch
        XCTAssertTrue(button.waitForExistence(timeout: 10))
        for _ in 0..<3 { button.tap() }
    }

    private func createFlow(named name: String) {
        app.tabBars.buttons["Flows"].tap()
        app.navigationBars["Flows"].buttons["Add"].tap()

        let field = app.textFields.element(boundBy: 0)
        XCTAssertTrue(field.waitForExistence(timeout: 5))
        field.tap()
        XCTAssertTrue(app.keyboards.element.waitForExistence(timeout: 5))
        field.typeText(name)

        app.buttons["Создать"].tap()
    }

    func testEmptyStateIsShown() {
        app.tabBars.buttons["Flows"].tap()
        XCTAssertTrue(app.staticTexts["Нет сценариев"].waitForExistence(timeout: 5))
    }

    func testCreatingFlowAddsItToTheList() {
        createFlow(named: "Checkout")
        XCTAssertTrue(app.staticTexts["Checkout"].waitForExistence(timeout: 5))
    }

    func testOpeningFlowShowsRunButton() {
        createFlow(named: "Checkout")
        app.staticTexts["Checkout"].tap()

        XCTAssertTrue(app.buttons["Запустить"].waitForExistence(timeout: 5))
    }

    func testRunButtonIsDisabledForEmptyFlow() {
        createFlow(named: "Empty")
        app.staticTexts["Empty"].tap()

        XCTAssertFalse(app.buttons["Запустить"].isEnabled)
    }
}
```

- [ ] **Step 3: Run the UI tests**

Run:
```bash
xcodebuild -project NetChecker-iOS/CheckerTestApp/CheckerTestApp.xcodeproj \
  -scheme CheckerTestApp \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -parallel-testing-enabled NO \
  -only-testing:CheckerTestAppUITests/FlowsUITests test
```

Expected: TEST SUCCEEDED, 4 tests

`-parallel-testing-enabled NO` is required: the simulator clones spawned for parallel runs fail to launch the test runner.

- [ ] **Step 4: Commit**

```bash
git add NetChecker-iOS/CheckerTestApp
git commit -m "test: cover the Flows screens with UI tests

The demo app gets a Flows tab so the tests can reach the screen without the
shake gesture."
```

---

## Task 11: Documentation and merge

**Files:**
- Modify: `README.md`
- Modify: `docs/superpowers/specs/2026-08-04-api-flows-design.md`

- [ ] **Step 1: Add the README section**

Insert before `### 📊 Programmatic Access`:

````markdown
### 🔗 API Flows

A feature usually spans several calls, and the fourth cannot be tested without
the first three: login returns a token, creating an order returns its id, and
only then does paying mean anything.

Build the chain from requests you already captured and run it with one tap:

```swift
// Steps are added from the traffic list — no code required.
// Values move between steps by name:
//   login   → token   → Authorization header of every later step
//   order   → orderId → /orders/{orderId}/pay
```

A step runs once everything it depends on has finished. That single rule covers
running two calls at once, waiting for both, and firing one off without waiting
for it at all. A step can carry a condition, so two steps depending on the same
parent become an if/else — exactly one runs.

When a step fails the run stops, and the values collected so far are kept. Retry
from that step resumes rather than starting over from login.

Flows export to a file, so a backend engineer can run the same scenario without
a TestFlight build. Sensitive header values are replaced on export.
````

- [ ] **Step 2: Mark the roadmap entry**

Change `- [ ] API flows` to `- [x] API flows — chain captured requests and run them in one tap` (add the line if absent).

- [ ] **Step 3: Update the spec status**

Change `**Статус:** утверждено` to `**Статус:** реализовано` and add under it:

```markdown
**Реализация:** ветка `feature/api-flows`, план — `docs/superpowers/plans/2026-08-04-api-flows.md`
```

- [ ] **Step 4: Full verification**

Run:
```bash
swift build && swift test
xcodebuild -project ../NetChecker-iOS/CheckerTestApp/CheckerTestApp.xcodeproj \
  -scheme CheckerTestApp -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -parallel-testing-enabled NO -only-testing:CheckerTestAppUITests test
```
Expected: build clean; all SDK tests pass; all UI tests pass

- [ ] **Step 5: Commit and merge**

```bash
git add README.md docs/superpowers/specs/2026-08-04-api-flows-design.md
git commit -m "docs: document API Flows"

git checkout development
git merge --squash feature/api-flows
git commit -m "feat: build and run API flows from captured requests

Chains of related calls can be assembled from captured traffic and executed with
one tap, replacing the workaround of hand-coding a mock scene and shipping it to
TestFlight for a backend engineer to try.

Steps form a DAG. A step runs once its dependencies finish and its condition
holds, which covers ordering, parallel start, wait-for-all and fire-and-forget
without a separate mode. Conditions turn two steps sharing a parent into an
if/else. Values extracted from responses are injected into later requests, and a
missing value fails the step by name instead of sending a blank substitution.

A failure stops the run but keeps the collected context, so retrying resumes at
the broken step. The graph is laid out in computed levels, so nothing is dragged
and scrolling stays vertical."

git branch -D feature/api-flows
git push origin development
```

---

## Self-Review

**Spec coverage.** Every section maps to a task: execution model → 3, 5; conditions → 2, 5; value passing → 1, 4; assertions → 3, 5; error behaviour and recovery → 4, 5, 9; layout → 7, 8; taps → 8, 9; assembly → 9; animation → 7; environment → inherited from `RequestRetrier`, which already routes through `EnvironmentStore`; export → 6; delay → 3, 5; storage → 6; file layout → matches §6 of the spec.

**Two spec items deliberately deferred**, both worth flagging before execution:

- The spec's §4.4 says tapping an edge opens a link editor. No task builds one, so links are only creatable in code. The graph, the runner and value passing are all testable without it, and an editor is a screen's worth of work that benefits from being designed against a working runner. It should be the next task after this plan lands.
- §4.5 mentions adding a whole tagged group at once. `FlowStore.makeFlow(name:from:)` accepts `[ArchivedRequest]` and is tested, so the capability exists; only the button is missing.

**Type consistency.** `FlowStep.request` is `RequestData` throughout; `FlowRunContext.prepared` throws and is wrapped in `Result` before crossing into the task group; `FlowStepState` is used identically in runner, node view and detail view; `InspectorFeature.flows` is added in Task 9 and consumed in the same task.

**One implementation detail is left for the implementer to resolve, and is called out in Task 5:** returning the response alongside the outcome instead of the `lastResponses` dictionary. The dictionary as written would be mutated from the task-group loop while steps run, which is exactly the kind of shared-state bug the runner is supposed to avoid. The tests catch it either way, but the tuple is the correct shape.
